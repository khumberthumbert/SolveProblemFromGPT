#include <stdio.h>

int main(void)
{
	int T = 0, x1 = 0, y1 = 0, x2 = 0, y2 = 0, r1 = 0, r2 = 0;
	scanf_s("%d", &T);

	for (int i = 0; i < T; i++)
	{
		scanf_s("%d %d %d %d %d %d",
			x1, y1, r1, x2, y2, r2);

	}

	return 0;
}